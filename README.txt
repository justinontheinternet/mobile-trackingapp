To start App:

In track-server directory, run: 'npm run dev' to get express server running

Must get new string from cloud.mongodb and past into index.js
Must also whitelist new IP Address if it has changed.